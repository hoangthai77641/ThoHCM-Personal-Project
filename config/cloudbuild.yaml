steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/thohcm-backend:$BUILD_ID'
      - './backend'
    id: 'build-backend'

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/thohcm-backend:$BUILD_ID'
    id: 'push-backend'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'thohcm-backend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/thohcm-backend:$BUILD_ID'
      - '--region'
      - 'asia-southeast1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '2Gi'
      - '--cpu'
      - '2'
      - '--max-instances'
      - '10'
      - '--min-instances'
      - '0'
      - '--timeout'
      - '900'
      - '--set-env-vars'
      - 'NODE_ENV=production,GCS_BUCKET_NAME=thohcm-storage,MONGODB_URI=${_MONGODB_URI},JWT_SECRET=${_JWT_SECRET},PORT=8080,HOST=0.0.0.0'
    id: 'deploy-backend'

# Store images in Container Registry
images:
  - 'gcr.io/$PROJECT_ID/thohcm-backend:$BUILD_ID'

# Build options
options:
  logging: CLOUD_LOGGING_ONLY